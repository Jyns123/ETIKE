# datasets crudos y limpios (pesan cientos de MB, no van a git)
home-credit-default-risk/*.csv
dataset/*.csv

# archivos del sistema / R
.DS_Store
.Rhistory

# secretos del pipeline
pipeline/.env

# backups de la BD (pesan cientos de MB, no van a git)
backups/

# bytecode compilado de Python
__pycache__/
*.pyc

# web: secretos, certificados (llaves privadas) y artefactos de build
web/backend/.env
web/certs/
web/frontend/node_modules/
web/frontend/dist/
