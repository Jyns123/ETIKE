PGHOST=localhost
PGPORT=5432
PGDATABASE=homecredit
PGUSER=postgres
PGPASSWORD=changeme
HC_ENCRYPTION_KEY=cambiar-esta-llave-larga-y-secreta

# Llave de los backups cifrados (backup_db.py), distinta de HC_ENCRYPTION_KEY.
# No escribirla a mano: python backup_db.py --generar-llave
# BACKUP_KEY=

# Opcional, solo si pg_dump/pg_restore del PATH no coinciden con la version
# del servidor (backup_db.py): carpeta bin del Postgres correcto.
# PG_BIN_DIR=/Library/PostgreSQL/16/bin
